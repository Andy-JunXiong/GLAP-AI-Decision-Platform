# GLAP Daily Development Log — August 2026

This is a lightweight evidence ledger, not a roadmap or authority source.
Historical details remain available in the preserved handoffs.

### 2026-08-17

Session work:

- Read-only Sites database and browser checks confirmed the first complete
  `human-evaluation-story.v2` submission at 09:16 Sydney time: 30 unique final
  answers, complete package digests, three attestations, and a locked submitted
  session. The superseded story-v1 draft remained isolated with three
  ineligible answers.
- Implemented a local multi-reviewer authentication extension. Each additional
  hosted secret account maps to a distinct pseudonymous reviewer ID, which
  scopes its own story-v2 session and answers. The legacy account remains
  compatible and no D1 schema or frozen review digest changed.
- Generated Dylan's dedicated credential material outside the repository. No
  plaintext password, email address, or hosted secret was written to source or
  documentation.
- Committed and pushed the multi-reviewer extension as GitHub commit `faa8861`;
  CI completed successfully. Appended the exact reviewer-site tree to the Sites
  source history as `39cf18e`, saved Sites v11, configured the second account
  in hosted secret revision 5, and deployed the version successfully.
- Ran a non-submitting canary with Dylan's account. Login, authenticated review
  read, and logout each returned 200; the response exposed the expected
  `human-evaluation-story.v2` collection and 30 packages with no session or
  answers. A post-canary D1 read retained two total sessions and zero sessions
  for Dylan's pseudonymous reviewer ID.
- Sent Dylan's credentials privately in the existing invitation thread only
  after the canary passed. The notice includes the formal entry, steps, safety
  prompts, and a no-reply-needed instruction.
- Generated Dong's dedicated credential material outside the repository and
  added it as the third hosted secret account without changing source, the
  frozen bundle, or D1. After correcting the pre-delivery salt encoding, Sites
  v11 environment revision 7 deployed successfully.
- Dong's corrected account returned 200 for login, authenticated review read,
  and logout; it exposed `human-evaluation-story.v2` and 30 packages with no
  session or answers. D1 retained two total sessions and zero Dong sessions.
- Sent Dong's credential notice privately only after the corrected canary
  passed. It includes the formal entry, steps, safety prompts, and a no-reply-
  needed instruction.
- A later read-only D1 check found Dong's own story-v2 session `SUBMITTED` with
  30 committed answer rows and all three attestations. Together with Ming, the
  collection now has two complete eligible-format submissions; no aggregation
  or Decision Quality result was produced.
- After explicit user confirmation, generated Xiaoshan's dedicated credential
  material outside the repository and added it as the fourth hosted secret
  account. Sites v11 environment revision 8 deployed successfully without a
  source, frozen-bundle, or D1 schema change.
- Xiaoshan's account returned 200 for login, authenticated review read, and
  logout; it exposed `human-evaluation-story.v2` and 30 packages with no
  session or answers. D1 retained zero Xiaoshan sessions.
- Sent Xiaoshan's credential notice privately only after the canary passed. It
  includes the formal entry, steps, safety prompts, and a no-reply-needed
  instruction.
- After explicit user confirmation, generated Linqi's dedicated credential
  material outside the repository and added it as the fifth hosted secret
  account. Sites v11 environment revision 9 deployed successfully without a
  source, frozen-bundle, or D1 schema change.
- Linqi's account returned 200 for login, authenticated review read, and
  logout; it exposed `human-evaluation-story.v2` and 30 packages with no
  session or answers. D1 retained zero Linqi sessions.
- Sent Linqi's credential notice privately only after the canary passed. It
  includes the formal entry, steps, safety prompts, and a no-reply-needed
  instruction.
- Added an aggregate-only Evaluation & Trust view to the GitHub Pages source.
  It reports ten replay cases, 30 frozen cutoffs, two of three complete eligible
  reviews, five isolated accounts, and `NOT EVALUATED` without exposing reviewer
  identities, credentials, answers, or private study artifacts. README,
  architecture, evaluation, replay, and demo-walkthrough documentation now use
  the same current facts and evidence boundaries.
- After explicit publication approval, committed the synchronized Demo and
  documentation as `5819e5549afd4d4bae46a905b4bf4800c41320ec` and pushed it
  directly to `main`. CI run `31990342255` and GitHub Pages run `31990342232`
  completed successfully. A post-deployment read of the public site confirmed
  the Evaluation navigation, `2 / 3`, `NOT EVALUATED`, and the dated aggregate-
  only evidence notice.
- Diagnosed the failed `2026-08-09` actual-calendar lifecycle continuation.
  Generation had succeeded and 27 of 28 lifecycle checks passed; only
  `missing_provider_coverage` failed because it required the later three-
  provider roadmap on a date when only the Maersk route was effective. Later
  attempts to restart from `2026-08-07` correctly failed before mutation
  because the persisted status date was newer.
- Implemented a recovery correction that compares booking coverage with
  active providers whose route configuration is effective on the logical date,
  and added safe existing/requested-date diagnostics. This does not manufacture
  historical provider rows or establish provider/model readiness.
- Inspected manual staging run `32012608848` after the human-authorized
  `deploy-recovery-controller` attempt failed. It used pushed commit `6b2a6c8`,
  passed repository tests, target isolation, and plan rendering, then stopped
  during idempotent schema application because the staging deployer's exact-
  resource Glue policy lacked `glue:GetTable` for one existing lifecycle
  catalog table. No seed was requested, and every downstream deployment,
  backfill, recovery, baseline, and Pages step was skipped.

Validation:

- Reviewer-site lint passed.
- Production build passed.
- All 23 site tests passed, including server-derived reviewer identity,
  authentication, immutable 30-package submission, bundle isolation, and
  unauthenticated client-exposure checks.
- All 17 focused public-demo tests and all 299 repository tests passed.
- Project drift audit passed 16/16; `git diff --check` and the approved-diff
  email/credential scan passed.
- The publication commit's pre-commit gate independently repeated compileall,
  all 299 repository tests, the 16-check drift audit, and staged-snapshot checks.
- The lifecycle recovery correction passed Python compilation, all 301
  repository tests, 16/16 project-drift checks, and `git diff --check` locally.
- Read-only Actions inspection confirmed run `32012608848` failed at the schema
  step and did not reach the temporal backfill, controller/stack deployment,
  failed-date recovery, baseline, or publication steps. Environment identifiers
  from the failure log were not copied into repository documentation.
- The incident-record closeout passed Python compilation, all 301 repository
  tests, 16/16 project-drift checks, and `git diff --check`; it performed no
  deployment retry, IAM change, data recovery, baseline refresh, or Pages
  publication.

Remaining:

- Dylan must personally provide only truthful attestations and judgments; no
  agent-created answer or submission is eligible.
- Xiaoshan must personally provide only truthful attestations and judgments; no
  agent-created answer or submission is eligible.
- Linqi must personally provide only truthful attestations and judgments; no
  agent-created answer or submission is eligible.
- Decision Quality remains `NOT_EVALUATED` and the benchmark gate remains
  `NOT_MET` until at least three complete independent reviews pass all checks.
- Lifecycle correction commit `1aeb0bbed29dff27d45451a8ba6a5f6ae32fb2da`
  was pushed to `main`; CI run `32011815316` passed. Its AWS release is now
  blocked by the missing exact-resource Glue table permission found in run
  `32012608848`. The repository policy inventory must be corrected and locally
  verified before a named IAM administrator separately reviews and applies a
  bounded policy update. Controlled `2026-08-09` recovery, continuation from
  `2026-08-10`, operational-baseline refresh, and Pages publication remain
  separate human-authorized steps.

Closeout:

- The previous Monday-Sunday window, 2026-08-10 through 2026-08-16, was checked
  at the first Monday closeout. Its detailed evidence is already preserved in
  this monthly ledger and the dated handoffs, so no duplicate archive was
  created.
- `DEVELOPMENT_PLAN.md` was unchanged because the durable P0-P5 direction and
  progression gates did not change.

### 2026-08-16

Session work:

- Reframed Human Evaluation around five distinct logistics decision stories:
  Baltimore port access, Panama Canal capacity allocation, Red Sea route
  governance, FAA network recovery sequencing, and Cyclone Gabrielle road
  safety.
- Added 15 sequential decision moments with business-facing event context,
  current operational facts, two anonymous executable choices, four comparison
  judgments, and confidence scoring.
- Removed hindsight contamination from the timeline: unrevealed nodes expose
  no future title, date, result label, colour cue, progress text, or tooltip;
  earlier judgments become read-only after later evidence is revealed.
- Verified every displayed moment against the frozen reviewer-safe bundle and
  retained seven expected identical pairs through an explicit allowlist rather
  than rewriting them for artificial contrast.
- Published Sites v7 after explicit human approval and sent the invited
  reviewer the direct preview link. The email clearly separated the preview
  from the formal v3 review.
- After the user made the separate formalization decision, implemented a local
  release candidate that routes `/pilot/human-evaluation` through the complete
  authenticated formal v3 client rather than the five-case preview.
- Preserved all frozen digests and formal requirements: ten cases, 30 packages,
  dedicated identity, three attestations, server save/resume, complete-package
  validation, and immutable final submission. Preview-local answers are never
  imported; the old presentation remains development-only.
- Committed and pushed the validated release candidate as GitHub commit
  `8858458`; CI run `31940302435` completed successfully.
- After separate explicit publication approval, synchronized the exact
  `blinded-review-survey` tree to the Sites source history as `b4334b6`, saved
  Sites v8, and deployed it publicly.
- Sent Ming a new formal-review notification only after the production canary
  passed. The message links directly to `/pilot/human-evaluation`, explains the
  ten-case/30-package scope, and states that the earlier browser-only preview
  does not migrate or count.
- User review then rejected the v8 numeric questionnaire interaction and
  required the formal route to retain the local preview's story hub,
  progressively revealed T0/T1/T2 moments, anonymous plans, and A/B/Tie
  judgments. Ming was immediately sent a correction asking them not to start
  v8; no replacement link has been sent.
- Implemented a corrected local candidate across all ten frozen cases. Each
  case has a distinct role, dependency, disruption progression, decision lens,
  and cutoff question; later cutoffs show only newly available facts. Formal
  judgments are stored in isolated story-review tables and committed moments
  are immutable.

Validation:

- Reviewer-site lint, production build, and all 21 site tests passed.
- Tests cover five cases, 15 moments, exact source mapping, control allowlisting,
  DOM-level future-information exclusion, strict sequential unlock, read-only
  past judgments, operational language, local persistence, and absence of the
  formal review API.
- The Sites deployment completed successfully. The preview retains a visible
  no-submission boundary and stores progress only in the current browser.
- No formal review answer, expert attestation, benchmark result, AWS mutation,
  operational Action, or Business Outcome Effect claim was created.
- The formal-integration candidate passed lint, production build, and all 22
  site tests. The added test verifies formal auth/save/submit wiring at the
  Human Evaluation route while retaining the development-only preview's
  isolation and future-information gates.
- A production-only dependency audit found the previous `next@16.2.6` inside
  the current official high-severity advisory range. The candidate was upgraded
  to Next.js `16.3.1`, rebuilt, and retested; its production-only npm audit now
  reports zero vulnerabilities.
- At the v8 checkpoint, the public root and formal route returned 200; the
  deployed client contains the formal submission badge, secure access flow,
  30-package scope, and review API; an unauthenticated API request returned 401.
- D1 remained unchanged after canary reads: three preserved draft sessions,
  zero answer rows, zero submitted sessions, and no progress beyond Case 1.
  The agent made no attestation, score, save, or submission.
- Corrected-candidate lint and production build passed, and all 23 reviewer-site
  tests passed. The API now enforces T0 -> T1 -> T2 within each story even if a
  client tries to bypass the interface. The corrected version remains local and
  remains undeployed and publicly uncanaried.
- Repository compilation and all 298 Python tests passed. The new comparative
  review tests verify weighted A/B/Tie aggregation, complete rubric dimensions,
  and rejection of mixed numeric/comparative submissions. The project drift
  audit passed 16 checks with zero drift.
- The user explicitly authorized commit and push of the corrected source. This
  does not authorize Sites publication, D1 migration, or a replacement message
  to Ming.
- The corrected source was committed and pushed as GitHub commit `e2f32b3`;
  CI run `31942757843` succeeded. After separate publication authority, its
  exact site tree was pushed as Sites source commit `c92b6a4`, saved as Sites
  v9, and deployed successfully at 21:05 Sydney time.
- Sites v9 applied migration `0003_rapid_misty_knight.sql`, creating isolated
  story-review session and answer tables while preserving the earlier
  questionnaire tables and drafts.
- A non-submitting production canary verified 200 login and authenticated review
  reads, collection `human-evaluation-story.v1`, comparative schema
  `decision-quality-comparative-review.v1`, 30 packages, ten distinct stories,
  three moments per story, and two anonymous options per package. The story
  tables remained at zero sessions and zero answers. The only event returned by
  the later error-filtered Worker query was the expected unauthenticated
  `/api/review` 401 probe, logged at info level with an `ok` Worker outcome.
- Only after all release and canary checks passed, sent Ming the corrected formal
  entry and instructions using the existing dedicated login. The agent made no
  attestation, judgment, save, or submission.
- Subsequent user inspection rejected Sites v9: it still exposed internal
  evaluation language and rendered identical frozen controls as duplicate A/B
  cards, making the task difficult to understand. Sites v9 is now paused for
  reviewer use; no eligible review evidence was claimed from it.
- Implemented a new local story v2 presentation. Each of the ten cases now has
  a plain-language role, opening situation, goal, stakes, three updates, three
  unknowns, and stage-specific actions and trade-offs. Internal cohort IDs,
  SLA labels, and raw option payloads are not shown to the reviewer.
- Identical frozen controls now render as one shared plan with an explicit
  confirmation instead of two identical cards. Distinct controls still render
  as anonymous A/B choices, with a runtime guard that rejects distinct source
  options if their reviewer-facing copy collapses to the same text.
- The new API collection is `human-evaluation-story.v2`, isolating the local
  candidate from any story-v1 session created after the v9 invitation.
- Local lint, production build, and all 23 reviewer-site tests pass. Automated
  browser screenshot capture was unavailable in the local Edge environment, so
  direct user review authorized the commit, publication, canary, and reviewer
  notification sequence; none of those release steps had occurred at this
  local-verification checkpoint.
- Committed and pushed the story-v2 implementation and synchronized release
  documentation to GitHub main as `cc26d95`. The changed paths trigger ordinary
  repository CI only, not AWS, Operations API, or GitHub Pages deployment.
- Appended the exact reviewer-site tree from `cc26d95` to the existing Sites
  source history as `21ba5e7`, saved Sites v10, and deployed it successfully at
  21:47 Sydney time.
- The non-submitting v10 canary returned 200 for the root, formal route, login,
  and authenticated review read; the unauthenticated review API returned 401.
  It verified collection `human-evaluation-story.v2`, comparative schema
  `decision-quality-comparative-review.v1`, 30 packages, ten three-moment
  stories, zero incomplete profiles, and fourteen shared-plan controls.
- D1 inspection before and after canary found zero story-v2 sessions and zero
  story-v2 answers. It also found one preserved story-v1 `DRAFT` at current
  index 3 with three answer rows, showing that Ming had begun the rejected v9
  flow. Those records remain isolated and ineligible and were not migrated.
- After canary success, sent Ming “GLAP 故事版正式测评已更新，请从案例 1 重新开始 /
  Story Review Updated,” directing them to Sites v10 with the existing login
  and explaining that only their new story-v2 work can count.

Remaining:

- Sites v8 and v9 remain superseded and ineligible; Sites v10 is the current
  canary-verified formal reviewer entry.
- Ming must personally restart from Case 1, exercise story-v2 save/resume, and
  complete only true attestations and judgments.
- Only the independent reviewer may enter scores or submit.
  Decision Quality stays `NOT_EVALUATED` and the benchmark gate remains
  `NOT_MET` until governed eligible submissions exist.

Closeout:

- `DEVELOPMENT_PLAN.md` was unchanged because the durable evaluation direction
  and progression gates did not change.
- The unrelated untracked repository-hero image files remain untouched.

### 2026-08-14

Session work:

- Implemented the Evaluation Architecture, Decision Quality review mechanics,
  and a ten-event Historical Replay corpus.
- Added the 2023 Gotthard road-tunnel closure as the sixth frozen scenario,
  extending coverage to ROAD and Europe while isolating the reopening notice
  as reveal-only evidence.
- Added the 2021 Ever Given grounding as the seventh frozen scenario, extending
  coverage to North Africa and vessel grounding while isolating refloating and
  restored navigation as reveal-only evidence.
- Added Cyclone Gabrielle's 2023 New Zealand state-highway disruption as the
  eighth frozen scenario, extending coverage to Oceania and extreme-weather
  road-network disruption while isolating partial recovery as reveal-only
  evidence.
- Added Singapore's 2024 container-port congestion as the ninth frozen
  scenario, extending coverage to Southeast Asia and port congestion while
  isolating the later reduction in berth wait time as reveal-only evidence.
- Added the 2024 Rio Grande do Sul flood-damaged federal-highway network as the
  tenth frozen scenario, extending coverage to South America while isolating
  the first BR-470 reopening as reveal-only evidence.
- Froze the exact ten-scenario corpus, ordered scenario bodies, and Decision
  Quality rubric by content digest, then added deterministic blinded packages
  for all 30 cutoffs plus a separately held study-owner key bundle.
- Added fail-closed tests for input drift, package integrity, option-identity
  leakage, reveal leakage, no-mutation authority, and compatibility with the
  pending Decision Quality scoring gate.
- Separated documentation into rules, durable direction, current truth, and
  archived history through Documentation Architecture v1.
- Explicitly did not deploy, publish Pages, call AWS, create expert reviews, or
  claim business outcome improvement.

Validation:

- The complete repository passed 293 Python tests and 16 drift checks after
  the review-freeze handoff.
- Fourteen focused Decision Quality and review-handoff tests passed, including
  six checks dedicated to the frozen 30-package corpus bundle.
- The deterministic corpus replay reported ten scenarios, 30 cutoffs, sixteen
  attributed changes, fourteen no-delta controls, all structural checks
  passing, and benchmark status `NOT_MET` because independent reviews are absent.
- Relative-link validation passed for 64 links across the nine changed Markdown
  files.

Remaining:

- Collect independent blinded reviews from the frozen handoff; repository tests
  cannot supply expert evidence.
- Preserve Action response-fix release, stable retry, and separate approval as
  pending human/runtime validation.

Closeout:

- Weekly archive check completed for the 3--9 August window: its session detail
  is already preserved in this monthly ledger and dated handoffs, while current
  authority remains in `CURRENT_DEVELOPMENT_STATUS.md`; no additional archive
  file was required.
- `DEVELOPMENT_PLAN.md` changes only when durable direction changes.
- `CURRENT_DEVELOPMENT_STATUS.md` becomes the source for Active Slice and Next
  Up.
- Historical handoffs and the former TODO/roadmap are archived rather than used
  as current authority.
- The migration was implemented and locally verified in this session. Commit
  and push maturity is recorded by Git history and the final user handoff; no
  deploy, publication, AWS call, or operational mutation occurred.
- The unrelated untracked user file remains untouched.

### 2026-08-13

Session work:

- Added Action assignment fields, applied the separately human-authorized
  staging schema migration, and recorded one named-operator `EDIT`.
- Diagnosed a response-only date serialization failure and implemented a fix.

Validation:

- Local tests and drift checks passed; staging schema and role checks passed.
- The response fix was pushed but not deployed.

Remaining:

- Narrow fix release, stable idempotent retry, and different-person approval or
  rejection.

Closeout:

- Detailed evidence: [`../handoffs/development_handoff_2026-08-13.md`](../handoffs/development_handoff_2026-08-13.md).

### 2026-08-10

Session work:

- Completed protected Prepare/Execute configuration and the narrow staging
  Action mutation release with failure recovery.

Validation:

- Reviewed artifact, change-set, stack, and Lambda evidence passed.

Remaining:

- The later Action assignment canary still required its separate sequence.

Closeout:

- Detailed evidence: [`../handoffs/development_handoff_2026-08-10.md`](../handoffs/development_handoff_2026-08-10.md).

### 2026-08-07

Session work:

- Completed the authenticated staging cockpit and governed Operations loop.

Validation:

- Auth, RBAC, API, UI, recovery, and private staging evidence were recorded.

Remaining:

- Actual-calendar provider coverage and production readiness remained blocked.

Closeout:

- Detailed evidence: [`../handoffs/development_handoff_2026-08-07.md`](../handoffs/development_handoff_2026-08-07.md).

### 2026-08-06

Session work:

- Established future-simulation and actual-calendar evidence boundaries.

Validation:

- Scenario mechanics were distinguished from operational performance evidence.

Remaining:

- Wait for eligible actual-calendar observations rather than rewrite history.

Closeout:

- Detailed evidence: [`../handoffs/development_handoff_2026-08-06.md`](../handoffs/development_handoff_2026-08-06.md).

### 2026-08-05

Session work:

- Completed stateful multimodal lifecycle and analytics foundations.

Validation:

- Local and isolated staging checks were recorded without enabling production.

Remaining:

- Accumulate actual-calendar provider and Outcome evidence.

Closeout:

- Detailed evidence: [`../handoffs/development_handoff_2026-08-05.md`](../handoffs/development_handoff_2026-08-05.md).
